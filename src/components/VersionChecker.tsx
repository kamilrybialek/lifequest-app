import React from 'react';

/**
 * VersionChecker Component for Native Platforms
 *
 * This is a stub component for iOS/Android.
 * Version checking is only needed for web/PWA platform.
 * Native apps update through app stores.
 */
export const VersionChecker: React.FC = () => {
  return null;
};
